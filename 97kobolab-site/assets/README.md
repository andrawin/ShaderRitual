Put the logo here as `97kobolab-logo.png`.

Use the original artwork — light grey wireframe on a white background. The site
inverts it in CSS (`filter: invert(1)`), so no editing or background removal is
needed.

If this file is absent the hero falls back to the typographic lockup, so the
page still renders correctly.
